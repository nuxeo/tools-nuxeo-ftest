<project>

  <properties>
    <log.config>log4j.xml</log.config>
    <log.dir>${project.build.directory}</log.dir>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skipTests>true</skipTests>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
            <configuration>
              <argLine>-Dlog4j.configuration=${file.uri.prefix}${basedir}/${log.config}
                -Dnuxeo.log.dir=${log.dir}
                -DconnectPassword=${connect.password}
                ${it.failsafe.argLine}
              </argLine>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
